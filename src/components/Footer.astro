---
const year = new Date().getFullYear();

const sections = [
  {
    title: 'Framework',
    links: [
      { href: '/framework/', label: 'QIF Overview' },
      { href: '/framework/#hourglass', label: 'Hourglass Model' },
      { href: '/framework/#coherence', label: 'Coherence Metric' },
    ],
  },
  {
    title: 'Resources',
    links: [
      { href: '/publications/', label: 'Publications' },
      { href: '/news/', label: 'News & Intel' },
      { href: '/lab/', label: 'QIF Lab' },
      { href: '/rss.xml', label: 'RSS Feed' },
    ],
  },
  {
    title: 'Connect',
    links: [
      { href: 'https://github.com/qinnovates', label: 'GitHub' },
      { href: 'https://medium.com/@qikevinl', label: 'Medium' },
      { href: '/about/', label: 'About' },
    ],
  },
];
---

<footer class="border-t border-[var(--color-border)] mt-24">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
    <div class="grid grid-cols-2 md:grid-cols-4 gap-8">
      <!-- Brand column -->
      <div class="col-span-2 md:col-span-1">
        <a href="/" class="text-xl font-bold font-[family-name:var(--font-heading)] text-[var(--color-text-primary)]">
          Qinnovate
        </a>
        <p class="mt-3 text-sm text-[var(--color-text-muted)] leading-relaxed">
          Advancing brain-computer interface security through the Quantum Indeterministic Framework.
        </p>
        <p class="mt-4 text-xs text-[var(--color-text-faint)]">
          QIF v3.1 Hourglass &middot; Open Framework
        </p>
      </div>

      {sections.map((section) => (
        <div>
          <h3 class="text-sm font-semibold text-[var(--color-text-primary)] uppercase tracking-wider mb-4">
            {section.title}
          </h3>
          <ul class="space-y-3">
            {section.links.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="text-sm text-[var(--color-text-muted)] hover:text-[var(--color-accent-primary)] transition-colors"
                  {...(link.href.startsWith('http') ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
                >
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>

    <div class="mt-12 pt-8 border-t border-[var(--color-border)] flex flex-col sm:flex-row justify-between items-center gap-4">
      <p class="text-xs text-[var(--color-text-faint)]">
        &copy; {year} Qinnovate. QIF is an open research framework.
      </p>
      <a
        href="/ctf/"
        class="text-xs text-[var(--color-text-faint)] hover:text-[var(--color-accent-tertiary)] transition-colors"
        title="Challenge yourself"
      >
        ?
      </a>
    </div>
  </div>
</footer>
